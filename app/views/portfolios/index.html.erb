<h1 style='margin-top:100px;font-size:2rem'>Our official portfolio directory (<%= link_to 'Click here to add a new Portfolio', new_portfolio_path %>)</h1>

<% @portfolios.each do |portfolio| %>
  <h1 style='font-size:1.5rem;margin-top:50px'>Portfolio Name:
    <%= link_to portfolio do  %>
      <%= portfolio.name %>
    <% end %>
  </h1>
  <p>Address: <%= portfolio.address %></p>
<% end %>
